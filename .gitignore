# ── Dependencies & build output ──────────────────────────────────────────────
node_modules/
build/
dist/
*.tsbuildinfo

# ── Logs ─────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Credentials & OAuth tokens — NEVER COMMIT ────────────────────────────────
# The client secret is shown once by Mural and grants API access on your behalf.
# The token cache holds a live access token and a long-lived refresh token.
.env
.env.*
!.env.example
tokens.json
*.tokens.json
.mural-mcp/
secrets.json
credentials.json
*.pem
*.key
*.p12
*.pfx

# ── Editor / OS noise ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# ── Local agent / tooling state ──────────────────────────────────────────────
.claude/worktrees/
.agents/
skills-lock.json

# ShowOff: ignore the heavy intermediates, but allow the finished video/poster
# to be committed so the README demo works on a fresh clone.
showoff-output/*/composition/
showoff-output/*/evidence-dossier.md
showoff-output/*/showoff-plan.md
